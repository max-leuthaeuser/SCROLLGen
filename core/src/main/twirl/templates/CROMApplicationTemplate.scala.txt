@(model: crom_l1_composed.Model)

@import generator.CROMGenerator
@import crom_l1_composed.Model

object CROMApplication extends App {
    @generator.CROMGenerator.getDataTypes(model).map(d => DataTypeTemplate(d, model).toString())
    @generator.CROMGenerator.getNaturalTypes(model).map(n => NaturalTypeTemplate(n, model).toString())
}